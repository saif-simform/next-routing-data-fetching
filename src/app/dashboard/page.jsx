import { preloadData } from "../lib/service";
import styles from "../ui/dashboard/dashboard.module.css";

const Dashboard = () => {
  return <div className={styles.wrapper}>Dashboard</div>;
};

export default Dashboard;
